Performance Build PB-0/PB-1: engine reliability + regression harness + intelligence contract

PB-0 — Regression harness (8 deterministic offline render tests, no audio device):
- silence_is_silent, launch_plays_source, master_path_is_transparent_at_unity
- commands_apply_at_exact_frame_within_block, direct_to_master_is_audible
- crossfade_routes_buses, stop_halts_playback, queue_overflow_failsafe

PB-1 — Audio engine reliability:
- Source ownership repaired: Launch carries Arc<DecodedBuffer> directly from
  the engine-thread registry; the callback loads it into the player. Removed
  the broken RegisterSource-through-queue path that never delivered buffers.
- Event-sliced rendering: commands scheduled mid-block now take effect at
  the exact requested frame, not the block boundary. Pending commands are
  sorted; rendering slices between event frames.
- Direct-to-master fixed: players on BusId::Master now actually reach the
  output mix (previously their audio was silently dropped; only metering
  accumulated).
- Transparent master path: removed the always-on x/(1+|x|) soft clipper.
  Output is hard-clamped at the DAC boundary only; unity playback null-tests
  clean. Clip detection retained for metering.
- TrackIntelligenceSnapshot: versioned, immutable contract between Core
  Intelligence (non-realtime) and the Performance Engine (realtime).
  Includes beat grid, 24-key posterior, alternatives, confidence tier
  (calibrated on FMAK model agreement), model agreement, and posterior-
  weighted harmonic compatibility function.

All 100 lib tests pass (91 existing + 9 new).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
